[pytest]
# Without this, a bare `pytest` can't find the `jobbot` package (only
# `python -m pytest` would work, since it adds the current directory to the path
# on its own).
pythonpath = .
testpaths = tests
