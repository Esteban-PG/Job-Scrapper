# Database of already-seen postings: it's local state, not code.
# On GitHub Actions it's persisted with actions/cache (see .github/workflows/).
data/seen_jobs.db

# Credentials. The versioned template is .env.example (always empty).
.env

# Working notes: they're for me, not for whoever visits the repo.
TODO.md
CANDIDATES.md
HOW-IT-WORKS.md

.venv/
__pycache__/
*.pyc
.pytest_cache/
.DS_Store
